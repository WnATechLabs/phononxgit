name: phononxgit
description: A new Flutter project. TODO
publish_to: 'none'

version: 0.0.1+1

# working flutter version:  3.10.1
environment:
  sdk: '>=3.0.1 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
